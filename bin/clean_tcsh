#!/bin/tcsh

set PYDIR = $INSTALLPATH_ROOT/python/3.7.9/gnu/9.1.0/bin
set MYDIR = `dirname $0`
set SPACK_STARTUP = `realpath $MYDIR/../share/spack/setup-env.csh`

exec env -i USER=$USER HOME=$HOME TERM=$TERM PATH=${PYDIR}:/usr/local/bin:/usr/bin:/bin TMPDIR=$TMPDIR /bin/tcsh -f
