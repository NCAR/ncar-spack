#!/bin/bash

PYDIR=$INSTALLPATH_ROOT/python/3.7.9/gnu/9.1.0/bin
SPACK_STARTUP="$( cd $(dirname "$0")/../share/spack ; realpath setup-env.sh )"

exec env -i USER=$USER HOME=$HOME TERM=$TERM PATH=$PYDIR:/usr/local/bin:/usr/bin:/bin TMPDIR=$TMPDIR /bin/bash --noprofile --rcfile $SPACK_STARTUP $@
